# 🚀 دليل النشر الكامل - ملك الماوية

## 📋 المحتويات
1. [إعداد Supabase](#إعداد-supabase)
2. [تنفيذ قاعدة البيانات](#تنفيذ-قاعدة-البيانات)
3. [النشر على Vercel](#النشر-على-vercel)
4. [الاختبار النهائي](#الاختبار-النهائي)

---

## 1️⃣ إعداد Supabase

### الخطوة 1: إنشاء مشروع جديد

1. **افتح [Supabase Dashboard](https://supabase.com/dashboard)**
2. **اضغط "New Project"**
3. **املأ البيانات:**
   ```
   Name: malek-mawia
   Database Password: [كلمة مرور قوية - احفظها!]
   Region: اختر الأقرب (مثلاً: Middle East)
   ```
4. **اضغط "Create new project"**
5. **انتظر 2-3 دقائق حتى يكتمل الإعداد**

### الخطوة 2: الحصول على المفاتيح

1. **من الـ Dashboard، اذهب إلى Settings → API**
2. **احفظ هذه المفاتيح:**

```bash
# Project URL
SUPABASE_URL=https://xxxxxxxx.supabase.co

# Anon/Public Key (للـ Frontend)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role Key (للـ Backend فقط - سري جداً!)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**⚠️ مهم جداً:**
- لا تشارك `SERVICE_ROLE_KEY` مع أحد!
- استخدم `ANON_KEY` فقط في Frontend
- `SERVICE_ROLE_KEY` للـ Backend API فقط

---

## 2️⃣ تنفيذ قاعدة البيانات

### الخطوة 1: فتح SQL Editor

1. **من Supabase Dashboard**
2. **اذهب إلى: SQL Editor**
3. **اضغط "+ New query"**

### الخطوة 2: تنفيذ الـ Schema

1. **افتح الملف:** `/database/schema.sql`
2. **انسخ المحتوى كاملاً** (2,000+ سطر)
3. **الصقه في SQL Editor**
4. **اضغط "Run"** (زر ▶️ أخضر)
5. **انتظر 10-15 ثانية**

### الخطوة 3: التحقق من النجاح

يجب أن ترى:
```
✅ Success. No rows returned
```

إذا ظهرت أخطاء:
- تأكد من نسخ الملف كاملاً
- تحقق من عدم وجود أحرف غريبة
- جرب تنفيذ الأقسام واحدة تلو الأخرى

### الخطوة 4: التحقق من الجداول

1. **اذهب إلى: Table Editor**
2. **يجب أن ترى 6 جداول:**
   ```
   ✅ users
   ✅ products
   ✅ sales
   ✅ debts
   ✅ payments
   ✅ activity_log
   ```

3. **افتح جدول users:**
   - يجب أن يكون هناك مستخدم واحد: **عبده ماوية**
   - Email: `admin@malek-mawia.ye`
   - Role: `admin`

4. **افتح جدول products:**
   - يجب أن يكون هناك 7 منتجات
   - جميعها active

---

## 3️⃣ النشر على Vercel

### الخطوة 1: تجهيز المشروع

1. **تأكد من وجود ملف `.gitignore`:**

```gitignore
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
```

2. **أنشئ ملف `.env.local` (للاختبار المحلي فقط):**

```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### الخطوة 2: رفع على GitHub

1. **إنشاء repository جديد:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: ملك الماوية system"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/malek-mawia.git
   git push -u origin main
   ```

### الخطوة 3: النشر على Vercel

1. **افتح [Vercel Dashboard](https://vercel.com/dashboard)**
2. **اضغط "Add New Project"**
3. **Import من GitHub:**
   - اختر repository: `malek-mawia`
4. **Configure Project:**
   ```
   Framework Preset: Next.js (auto-detected)
   Root Directory: ./
   Build Command: npm run build (auto)
   Output Directory: .next (auto)
   ```

5. **Environment Variables:**

   **أضف هذه المتغيرات:**
   ```
   NEXT_PUBLIC_SUPABASE_URL = https://xxxxxxxx.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   SUPABASE_SERVICE_ROLE_KEY = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

   **⚠️ مهم:**
   - استخدم `NEXT_PUBLIC_` للمتغيرات التي يحتاجها Frontend
   - بدون `NEXT_PUBLIC_` للمتغيرات السرية (Backend فقط)

6. **اضغط "Deploy"**
7. **انتظر 2-3 دقائق**
8. **🎉 تم! موقعك الآن حي على:**
   ```
   https://malek-mawia.vercel.app
   ```

---

## 4️⃣ الاختبار النهائي

### اختبار 1: تسجيل الدخول

1. **افتح الموقع**
2. **تسجيل الدخول:**
   ```
   Email: admin@malek-mawia.ye
   Password: admin123
   ```
3. **✅ يجب أن تدخل بنجاح**

**⚠️ مهم:** غيّر كلمة المرور فوراً!

### اختبار 2: المساعد الذكي - حساب يدوي

1. **افتح "المساعد الذكي"**
2. **Tab "حساب يدوي":**
   - نوع القات: **طوفان**
   - الكمية: **5**
   - السعر: **1000**
   - الحالة: **نقداً**
3. **اضغط "تسجيل البيع"**
4. **✅ يجب أن ترى:**
   - رسالة نجاح خضراء
   - النموذج يُمسح
5. **اذهب إلى "المبيعات"**
6. **✅ يجب أن ترى البيع المسجل**

### اختبار 3: رفع صورة

1. **حضّر صورة بهذا المحتوى:**
   ```
   طوفان 5 × 1000
   حسين 3 - 500
   ```
2. **Tab "رفع صور"**
3. **ارفع الصورة**
4. **اضغط "تحليل الصورة"**
5. **انتظر 15-20 ثانية**
6. **✅ يجب أن ترى:**
   - النص المستخرج
   - 2 عملية مستخرجة
   - المجموع: 6,500 ريال
7. **اضغط "حفظ جميع العمليات"**
8. **✅ يجب أن تُحفظ في قاعدة البيانات**

### اختبار 4: الدردشة

1. **Tab "دردشة"**
2. **اكتب: "تقرير اليوم"**
3. **✅ يجب أن يرد بملخص المبيعات**
4. **جرب أوامر أخرى:**
   - "الديون"
   - "المنتجات"
   - "إجمالي المبيعات"

### اختبار 5: التصدير

1. **اذهب إلى "المبيعات"**
2. **اضغط "PDF"**
3. **✅ يجب أن يُحمل ملف PDF**
4. **افتح الملف**
5. **✅ تأكد من:**
   - العربي يظهر صحيح
   - الجدول منظم
   - البيانات صحيحة

### اختبار 6: الديون

1. **سجل بيع جديد بحالة "دين"**
2. **أدخل اسم زبون**
3. **اضغط "تسجيل"**
4. **اذهب إلى "الديون"**
5. **✅ يجب أن ترى الدين**
6. **اضغط "تسجيل دفعة"**
7. **أدخل نصف المبلغ**
8. **✅ الحالة تتغير إلى "جزئي"**

---

## 🔐 الأمان

### تفعيل Row Level Security (RLS)

RLS مفعّل تلقائياً في الـ schema، لكن تحقق:

1. **من Supabase Dashboard**
2. **اذهب إلى: Authentication → Policies**
3. **تأكد من أن كل جدول له Policies**

### الصلاحيات:

```
🔴 Admin (المدير):
   ✅ كل الصلاحيات
   ✅ إضافة/تعديل/حذف كل شيء
   ✅ عرض جميع البيانات
   ✅ إدارة المستخدمين

🟡 Supervisor (المشرف):
   ✅ عرض كل المبيعات والديون
   ✅ تسجيل مبيعات جديدة
   ✅ تحديث الديون
   ❌ حذف أي شيء
   ❌ إدارة المستخدمين

🟢 Seller (البائع):
   ✅ تسجيل مبيعات جديدة
   ✅ عرض مبيعاته فقط
   ❌ عرض مبيعات الآخرين
   ❌ تعديل/حذف
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: "Connection failed"

**الحل:**
```bash
1. تحقق من SUPABASE_URL
2. تأكد من أن المشروع active في Supabase
3. جرب الاتصال من Supabase Dashboard → SQL Editor
```

### المشكلة 2: "Unauthorized"

**الحل:**
```bash
1. تحقق من SUPABASE_ANON_KEY
2. تأكد من تفعيل RLS
3. راجع الـ Policies
```

### المشكلة 3: OCR لا يعمل

**الحل:**
```bash
1. الصورة واضحة؟
2. الصيغة صحيحة؟ (المنتج الكمية × السعر)
3. حجم الصورة < 10MB؟
4. جرب صورة أخرى
```

### المشكلة 4: التصدير لا يعمل

**الحل:**
```bash
1. تحقق من Console للأخطاء
2. هل هناك بيانات؟
3. جرب في متصفح آخر
4. امسح Cache
```

---

## 📊 المراقبة والصيانة

### النسخ الاحتياطي التلقائي

Supabase يقوم بنسخ احتياطي تلقائي كل يوم.

**للوصول:**
1. Supabase Dashboard → Database → Backups

### النسخ الاحتياطي اليدوي

```bash
# استخدم pg_dump
pg_dump -h db.xxxxxxxx.supabase.co -U postgres -d postgres > backup.sql
```

### مراقبة الأداء

1. **Supabase Dashboard → Reports**
2. **شاهد:**
   - Database size
   - API requests
   - Error rate

---

## 🎯 الخلاصة

✅ **ما تم:**
1. إعداد Supabase
2. تنفيذ قاعدة البيانات (6 جداول)
3. النشر على Vercel
4. الاختبار الكامل

✅ **النظام الآن:**
- 🌐 حي على الإنترنت
- 🔒 آمن ومشفر
- 📊 قاعدة بيانات حقيقية
- 🚀 سريع جداً
- 🆓 100% مجاني

✅ **جاهز للاستخدام:**
- المساعد الذكي (3 في 1)
- رفع الصور (OCR)
- الدردشة الذكية
- التصدير (PDF, Excel, CSV)
- إدارة المستخدمين
- نظام الصلاحيات

---

## 📞 الدعم

### إذا واجهت مشكلة:

1. **تحقق من Console:**
   ```
   F12 → Console → شاهد الأخطاء
   ```

2. **Supabase Logs:**
   ```
   Dashboard → Logs → API
   ```

3. **Vercel Logs:**
   ```
   Vercel Dashboard → Logs
   ```

---

## 🎉 تهانينا!

**نظام ملك الماوية الآن:**
- ✅ جاهز للإنتاج
- ✅ مربوط بقاعدة بيانات حقيقية
- ✅ منشور على الإنترنت
- ✅ آمن ومشفر

**استمتع بالنظام! 🚀🎊**

---

**صنع بـ ❤️ للمدير العام: عبده ماوية**
